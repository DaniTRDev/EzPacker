# x86-64 target subproject.
#
# Target libraries, DSL inputs and build-time plugin are defined here. This scaffold is
# populated as the x86-64 runtime, emitter and DSL plugin are migrated out of the core
# projects.
