# Module with parameters
# Tests: parameter parsing, symbol definition, type resolution

i64 ModuleWithParams(i32 %paramA, i64 %paramB, i32 %paramC)
{
    create i64 %local;
    mov %local, %paramB;
    add %local, 1;
    nop;
}

